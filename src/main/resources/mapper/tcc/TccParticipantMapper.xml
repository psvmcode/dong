<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dong.tcc.mapper.TccParticipantMapper">

    <resultMap id="inventoryMap" type="com.dong.tcc.entity.TccInventory">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="available" property="available"/>
        <result column="frozen" property="frozen"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="accountMap" type="com.dong.tcc.entity.TccAccount">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="balance" property="balance"/>
        <result column="frozen" property="frozen"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="orderMap" type="com.dong.tcc.entity.TccOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="xid" property="xid"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="amount" property="amount"/>
        <result column="status" property="status" javaType="com.dong.tcc.enums.TccOrderStatus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 查库存 -->
    <select id="selectInventory" resultMap="inventoryMap">
        select id, product_id, available, frozen, create_time, update_time
        from tcc_inventory
        where product_id = #{productId}
    </select>

    <!-- 初始化库存 -->
    <insert id="insertInventory" parameterType="com.dong.tcc.entity.TccInventory"
            useGeneratedKeys="true" keyProperty="id">
        insert into tcc_inventory (product_id, available, frozen)
        values (#{productId}, #{available}, #{frozen})
    </insert>

    <!-- Try 阶段冻结库存 -->
    <update id="freezeInventory">
        update tcc_inventory
        set available = available - #{quantity},
            frozen    = frozen + #{quantity}
        where product_id = #{productId}
          and available &gt;= #{quantity}
    </update>

    <!-- Confirm 阶段扣减冻结库存 -->
    <update id="confirmInventory">
        update tcc_inventory
        set frozen = frozen - #{quantity}
        where product_id = #{productId}
          and frozen &gt;= #{quantity}
    </update>

    <!-- Cancel 阶段释放冻结库存 -->
    <update id="cancelInventory">
        update tcc_inventory
        set available = available + #{quantity},
            frozen    = frozen - #{quantity}
        where product_id = #{productId}
          and frozen &gt;= #{quantity}
    </update>

    <!-- 查账户 -->
    <select id="selectAccount" resultMap="accountMap">
        select id, user_id, balance, frozen, create_time, update_time
        from tcc_account
        where user_id = #{userId}
    </select>

    <!-- 初始化账户 -->
    <insert id="insertAccount" parameterType="com.dong.tcc.entity.TccAccount"
            useGeneratedKeys="true" keyProperty="id">
        insert into tcc_account (user_id, balance, frozen)
        values (#{userId}, #{balance}, #{frozen})
    </insert>

    <!-- Try 阶段冻结余额 -->
    <update id="freezeAccount">
        update tcc_account
        set balance = balance - #{amount},
            frozen  = frozen + #{amount}
        where user_id = #{userId}
          and balance &gt;= #{amount}
    </update>

    <!-- Confirm 阶段扣减冻结余额 -->
    <update id="confirmAccount">
        update tcc_account
        set frozen = frozen - #{amount}
        where user_id = #{userId}
          and frozen &gt;= #{amount}
    </update>

    <!-- Cancel 阶段释放冻结余额 -->
    <update id="cancelAccount">
        update tcc_account
        set balance = balance + #{amount},
            frozen  = frozen - #{amount}
        where user_id = #{userId}
          and frozen &gt;= #{amount}
    </update>

    <!-- 按全局事务查订单 -->
    <select id="selectOrderByXid" resultMap="orderMap">
        select id, order_no, xid, user_id, product_id, quantity, amount, status, create_time, update_time
        from tcc_order
        where xid = #{xid}
    </select>

    <!-- 创建订单 -->
    <insert id="insertOrder" parameterType="com.dong.tcc.entity.TccOrder"
            useGeneratedKeys="true" keyProperty="id">
        insert into tcc_order (order_no, xid, user_id, product_id, quantity, amount, status)
        values (#{orderNo}, #{xid}, #{userId}, #{productId}, #{quantity}, #{amount}, #{status})
    </insert>

    <!-- 推进订单状态 -->
    <update id="updateOrderStatus">
        update tcc_order
        set status = #{status}
        where xid = #{xid}
    </update>

</mapper>
