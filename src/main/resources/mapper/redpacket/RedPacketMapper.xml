<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dong.redpacket.mapper.RedPacketMapper">

    <resultMap id="redPacketMap" type="com.dong.redpacket.entity.RedPacket">
        <id column="id" property="id"/>
        <result column="packet_no" property="packetNo"/>
        <result column="sponsor_id" property="sponsorId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="total_count" property="totalCount"/>
        <result column="remain_amount" property="remainAmount"/>
        <result column="remain_count" property="remainCount"/>
        <result column="packet_type" property="packetType" javaType="com.dong.redpacket.enums.RedPacketType"/>
        <result column="status" property="status" javaType="com.dong.redpacket.enums.RedPacketStatus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="recordMap" type="com.dong.redpacket.entity.RedPacketRecord">
        <id column="id" property="id"/>
        <result column="packet_no" property="packetNo"/>
        <result column="user_id" property="userId"/>
        <result column="amount" property="amount"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 按红包编号查红包 -->
    <select id="selectByPacketNo" resultMap="redPacketMap">
        select id, packet_no, sponsor_id, total_amount, total_count, remain_amount, remain_count, packet_type, status,
        create_time, update_time
        from red_packet
        where packet_no = #{packetNo}
    </select>

    <!-- 发红包落库 -->
    <insert id="insert" parameterType="com.dong.redpacket.entity.RedPacket"
            useGeneratedKeys="true" keyProperty="id">
        insert into red_packet (packet_no, sponsor_id, total_amount, total_count, remain_amount, remain_count,
        packet_type, status)
        values (#{packetNo}, #{sponsorId}, #{totalAmount}, #{totalCount}, #{remainAmount}, #{remainCount},
        #{packetType}, #{status})
    </insert>

    <!-- 推进红包状态，已处于目标状态就不再写库 -->
    <update id="updateStatus">
        update red_packet
        set status = #{status}
        where packet_no = #{packetNo}
        and status &lt;&gt; #{status}
    </update>

    <!-- 扣减剩余金额与份数。充足条件写在 where 里是防超发的关键：
         并发下若库存已不足，更新影响行数为 0，由调用方判定为抢完而不是扣成负数 -->
    <update id="decreaseRemain">
        update red_packet
        set remain_amount = remain_amount - #{amount},
        remain_count = remain_count - #{count}
        where packet_no = #{packetNo}
        and remain_count &gt;= #{count}
        and remain_amount &gt;= #{amount}
    </update>

    <!-- 查领取记录 -->
    <select id="selectRecords" resultMap="recordMap">
        select id, packet_no, user_id, amount, create_time
        from red_packet_record
        where packet_no = #{packetNo}
        order by id asc
    </select>

    <!-- 写领取记录 -->
    <insert id="insertRecord" parameterType="com.dong.redpacket.entity.RedPacketRecord"
            useGeneratedKeys="true" keyProperty="id">
        insert into red_packet_record (packet_no, user_id, amount)
        values (#{packetNo}, #{userId}, #{amount})
    </insert>

    <!-- 统计领取记录数 -->
    <select id="countRecord" resultType="java.lang.Integer">
        select count(1)
        from red_packet_record
        where packet_no = #{packetNo}
        and user_id = #{userId}
    </select>

    <!-- 已领取用户 id，重建 Redis 去重集合时使用 -->
    <select id="selectClaimedUserIds" resultType="java.lang.Long">
        select user_id
        from red_packet_record
        where packet_no = #{packetNo}
    </select>

    <!-- 按未领取份额回写剩余，以份额表为准 -->
    <update id="updateRemain">
        update red_packet
        set remain_amount = #{remainAmount},
        remain_count = #{remainCount}
        where packet_no = #{packetNo}
    </update>

    <!-- 未结束的红包，对账任务按主键顺序扫描 -->
    <select id="selectUnfinished" resultMap="redPacketMap">
        select id, packet_no, sponsor_id, total_amount, total_count, remain_amount, remain_count, packet_type, status,
        create_time, update_time
        from red_packet
        where status in (0, 1)
        order by id asc
        limit #{limit}
    </select>

</mapper>
