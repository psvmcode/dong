<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dong.lab.replica.mapper.UserAccountMapper">

    <resultMap id="accountMap" type="com.dong.lab.replica.entity.UserAccount">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="balance" property="balance"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByUserId" resultMap="accountMap">
        select id, user_id, username, balance, create_time, update_time
        from user_account
        where user_id = #{userId}
    </select>

    <select id="selectAll" resultMap="accountMap">
        select id, user_id, username, balance, create_time, update_time
        from user_account
        order by id asc
    </select>

    <insert id="insert" parameterType="com.dong.lab.replica.entity.UserAccount"
            useGeneratedKeys="true" keyProperty="id">
        insert into user_account (user_id, username, balance)
        values (#{userId}, #{username}, #{balance})
    </insert>

    <update id="updateBalance">
        update user_account
        set balance = #{balance}
        where user_id = #{userId}
    </update>

</mapper>
