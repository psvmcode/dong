# 2026-09-05

- 新增 `order` 模块（订单履约状态机），选型经用户确认：电商订单履约场景 + COLA StateMachine。
  依赖 `com.alibaba.cola:cola-component-statemachine:5.0.0`，commit 833d0d4 已推送。
  内容：7 状态 9 事件、守卫、内部迁移催单、退款失败条件分支回退、终态拦截、流转日志、并发对比实验。
- 实测结论（16 线程并发推进同一订单，各跑 5 轮）：`mode=cas` 的 successCount 恒为 1、finalVersion 恒为 2；
  `mode=none` 的 successCount 在 13~16 浮动、finalVersion 恒为 1。README 初稿误写成固定的 16，已按实测分布改正。
- 已清理遗留：`tcc/mapper/TccStatusTypeHandler.java` 0 字节残留文件（delete_file 只清空不删除，第三次踩到）。
  全项目零引用，`rm -f` 删除后 git status 显示 `D` 确认真删，编译通过，commit 217d760 已推送。
- 应用户要求规整包结构（commit dc72238，25 个文件）：`service` 包清出 7 个非接口类，
  分别归入 `task`（定时任务 3 个）、`handler`（消息处理器 2 个）、`support`（工具与本地组件 2 个）；
  `framework` 的 cache/limiter/mq/lock 四组接口与实现拆开，实现进各自 `impl` 子包。
  `CacheEntry` 因跨包改为 public。README 分层约定与目录树同步更新。
- 回归验证：启动后逐模块冒烟，缓存（预热/查询/统计/穿透）、限流、分布式锁、MQ、红包（发抢余金额守恒）、
  秒杀、TCC（提交成功 + 恢复任务）、跨境开户、订单状态机完整流程全部 code=0，与重构前一致。
- 新增 `doc/crossborder-payment.md`（跨境支付专项学习文档，501 行，commit b3c8e8b）：以业务为主线，
  覆盖五大难题、九阶段流程、概念速查、8 张表模型、汇款单状态机、四道合规检查、7 个关键工程实现、
  38 个接口清单、可直接复制的实操命令，数据全部实测。README 已加导航并修正不准确的描述。
- 顺带修复真实缺陷：`cross_border_account_event` 表在 schema.sql 与远程库中都不存在，
  但 `AccountEventMapper` 一直在写它 → 冻结、解冻、事件查询三个接口必然报"表不存在"。
  已补 DDL 并在远程库建表，实测冻结/解冻/事件查询恢复正常，重复冻结正确报冲突。
- 发现业务规则冲突：开户默认 `singleLimit=50000` 恰好等于 AML 人工审核阈值 50000，
  导致人工审核分支默认不可达（超 5 万先被限额 REJECT，AML 轮不到返回 MANUAL_REVIEW）。
  演示该分支必须开户时指定更大的 singleLimit。README 原有描述有误，已修正。
- 排查用户报的启动失败（`Cannot resolve reference to bean 'replicaSqlSessionFactory'`）：非代码问题。
  我这边 `mvn clean` 从零构建启动成功，replica 写入与一致性检查均通过、health 显示 replicaDataSource UP。
  根因是 `new HikariDataSource(config)` 构造即建连，MariaDB(3307) 瞬时不可用会连锁导致该报错。
  已 clean 并重启，应用在 8090 正常运行。
- 应用户要求压缩中间件内存（commit 1ffd45f，改 deploy/docker-compose.yml 与 lab.sh）：
  MySQL 512m→320m、Redis 320m→128m、MariaDB 384m→256m、ES 900m→800m、MongoDB 512m→400m、
  RocketMQ namesrv 220m / broker 420m、Kafka 512m。YAML 已用 ruby 校验通过。
  注意：无 SSH 权限，改动未经服务器实测，需用户同步到 /opt/dong-lab 后重建容器生效。
- 中间件内存占用排序（当前配置下，从大到小）：ES > RocketMQ(namesrv+broker) > Kafka > MongoDB > MySQL > MariaDB > Redis。
  2G 服务器建议常驻 core（MySQL+Redis，约 450m），其余按需 `lab.sh <profile> up` 用完即 down。
- 上述内存配置与资源预算已写入 README 第七节「内存是硬约束」（commit 0b868b3）：
  含各组件 mem_limit 与关键参数表、保留优先级、六种组合可行性预算、重建容器生效命令；
  同时修正了「使用」小节里过期的估算数字。
- 用户提供服务器密码后完成线上清理与重建（commit 493a09a、9f57357）：
  ① 删除两个孤儿 broker 容器 great_hamilton / goofy_lehmann（271MB，手工启动无 compose 标签）；
  ② 原以为宿主机另装了 ES(484MB) 与 MySQL(31MB)，经 cgroup + docker top 核实**实为容器内进程**，
     因 uid 映射显示为 lighthouse / polkitd 而误判，幸未执行 kill；
  ③ /opt/dong-lab 缺 .env，已上传本地 deploy/.env（密码与运行中的容器一致）；
  ④ 按压缩配置重建全部容器，暴露 broker 的 envsubst 写 /home/rocketmq/conf 权限问题
     （镜像无该目录且容器以 uid 3000 运行），改为渲染到 /tmp 后启动成功，brokerIP1 正确渲染为公网 IP。
- 实测（全部中间件同时运行）：ES 616MB、broker 266MB、MySQL 114MB、MongoDB 98MB、
  MariaDB 44MB、namesrv 39MB、Redis 7MB，容器合计约 1.2G；系统 used 1.4G、available 约 400MB、
  swap 从 2742MB 降至 727MB。结论修正为「可以长期全开，仅 ES 值得斟酌」。
  端到端回归：冒烟全过，跨境对账 localCount=2 matchedCount=2 balanced=true（RocketMQ 链路正常）。
- 注意：含明文密码的 expect 临时脚本已删除；建议用户尽快轮换 root 密码并收紧安全组。
- 已配置 SSH 密钥免密登录：复用本机 ~/.ssh/id_ed25519（原用于 GitHub），公钥已追加到服务器
  ~/.ssh/authorized_keys（权限 600、目录 700），并在 ~/.ssh/config 增加别名 lab。
  实测 `ssh lab` 与 `ssh -T git@github.com` 均正常，二者共用同一把密钥互不干扰。
  未禁用密码登录（有锁死风险，需用户决定）。
- 为 db/schema.sql 全部 DDL 补中文注释（commit 3748e69）：25 张表加表级 comment，
  225 个字段与索引加中文注释，状态字段写明枚举取值。验证方式：用 perl 把库名替换成
  dong_lab_chk / dong_lab_replica_chk 生成副本，在服务器独立临时库执行，
  再查 information_schema 核对覆盖率（225/225、25/25），最后删除临时库。
- 已将注释同步到现有库（用户批准，分三步，每步先备份）：
  ① `mysqldump --no-data --skip-add-drop-table` 备份两库结构到 `/opt/dong-lab/backup/`
     （加 --skip-add-drop-table，避免恢复时误删表）；
  ② 表注释 25 条 `alter table t comment = '...'`（仅改元数据）→ dong_lab 24/24、replica 1/1；
  ③ 字段注释按表分 25 批逐条执行 → 全部成功，字段 225/225（dong_lab 219 + replica 6）。
  验证：数据零丢失（trade_order 22 行、user_account 5 行不变），
  `show create table` 注释正确，应用冒烟与 replica 读写转账均正常。
- 批量改字段注释的技巧：用 perl 从 schema.sql 解析「字段名 / 定义 / 注释」，
  **每表拼成一条 ALTER**（`alter table t modify column a ... comment '..', modify column b ...`），
  25 条语句即可改完 225 个字段，且每张表只重建一次，比 225 条独立语句快得多。
- MEMORY.md 超限被截断，已合并去重重写：删除旧微服务 HR/财务/前端等已失效细节（只留一行归档说明），
  保留 dong-lab 现状 + 坑位清单，长度压缩约 60%。
- 应用户要求为 `doc/crossborder-payment.md` 新增第〇节「零基础白话版业务流程」（commit 39cfc54，+373 行）：
  用「寄国际包裹」类比讲清换汇→送钱→审查→对账，十步主线 + A~M 共 13 类异常分支 + 一页速查表 + 三句总结。
  写作前逐行核对了 RemittanceServiceImpl / ComplianceServiceImpl / FxQuoteServiceImpl /
  CrossBorderLedgerServiceImpl / SettlementHandler / MaintenanceTask / ReconciliationServiceImpl，
  所有数字与分支判定均以代码为准。
  顺带确认一处留白：`failAndRefund` 只有接口与实现，**无 Controller 端点、也无自动触发点**
  （与文档第十一节「未实现退汇」是同一类缺口），已在白话版 H 条如实标注。
- 修复真实缺陷并端到端验证（commit 819be00，7 文件 +250/-44）：`SETTLING` 状态此前**只被读、从未被写入**
  （仅 RemittanceStatus 枚举定义 + FxExposureServiceImpl 查询），文档状态机图画的两段式清算名不副实。
  改造：新增 `CrossBorderLedgerService.markSettling()`（`FUNDS_DEBITED → SETTLING`，不碰钱）；
  `creditAndAdvance()` 改为要求 `SETTLING` 才能推进到 `SETTLED`，内部重新查库拿最新 version；
  handler 允许 `FUNDS_DEBITED/SETTLING` 进入、**归批只在 FUNDS_DEBITED 时做一次**（否则消息重发会把批次金额重复累加）；
  `SettlementServiceImpl.settle()` 同步改为两步；补偿任务扫描纳入 `SETTLING`（只扫 FUNDS_DEBITED 会永久漏掉已投递未入账的单子）。
  可观测性：`runtime` 新增 `settlingEntered` 累计计数（瞬时状态数永远是 0），
  为此 `RemittanceServiceImpl` 注入 `CrossBorderSettlementHandler`（handler 不依赖 RemittanceService，无循环依赖）。
- 改造过程中实测发现第二个并发缺陷：手动清算与消息消费撞车时 `settle` 抛异常（data 为 null）。
  根因是 `markSettling` 对 `SETTLED` 终态抛 BusinessException，而兜底清算遇到它应该跳过。
  修为「`SETTLING`/`SETTLED` 都返回 false，`creditAndAdvance` 对 `SETTLED` 也返回 false（本次未结算，不计入笔数）」。
  修复后实测两条路径并发：只有一方真正推进，另一方优雅跳过，批次金额未重复累加。
- 本次验证结论（全部在云服务器中间件上实跑）：4 笔汇款 + 审核放行 + 冻结解冻 + 制裁 + 幂等重放全部通过；
  三方账户 `diff=0.00 consistent=true`；`FUNDS_DEBITED=0 SETTLING=0` 无卡单；`settlingEntered=2`。
  教训：`runtime` 接口依赖 Redis（`sanctionCount`），云服务器 Redis 偶发 `command timed out` 会导致整个接口 500，需重试。
  另：`GET /accounts/{id}` 不存在，按 id 查账户只能用 `GET /accounts` 列表过滤（路径 `/accounts/{accountNo}` 只认账号）。
- 理清三份 DDL 的关系并消除重复维护（commit 9c3c0b2）：
  `db/schema.sql`（460 行，25 表）是**唯一权威源**，自带 `create database` + `use dong_lab`（第 3 行）
  → `use dong_lab_replica`（第 400 行）→ `use dong_lab`（第 416 行，切回），即主库的表**分散在两段**、中间夹着 replica 段。
  `deploy/initdb/01-schema.sql` 挂 MySQL 容器、`deploy/initdb-replica/01-schema.sql` 挂 MariaDB 容器，
  二者**必须**剥掉 `create database`/`use`——`/docker-entrypoint-initdb.d` 只在数据目录为空时执行一次，
  执行前已进入 `MYSQL_DATABASE`/`MARIADB_DATABASE` 指定的库（compose 里是 dong_lab / dong_lab_replica）。
  所以三份不是重复，但 initdb 手工维护必然漂移：**实测 initdb 缺 `trade_order`、`trade_order_transition_log`**
  （9-05 新增 order 模块时只改了 schema.sql 与真实库）。此缺口平时测不出（容器早已初始化，脚本不会再跑），
  只在删 volume 全新部署时才炸。已新增 `deploy/gen-initdb.sh`：awk 按 `use` 切分并剥掉建库语句，
  同名库多次出现自动追加（正好处理主库分两段的情况），再 tidy 空行（去首尾空行、压连续空行、末尾单个换行）。
  生成后 22→24 表，README 已登记「加表只改 db/schema.sql 然后跑 gen-initdb.sh」。
  验证：MySQL 3306 临时库 24 表 / 24 表注释 / 219 字段注释；MariaDB 3307 临时库 1 表 / 1 / 6（合计 225，与历史核对一致）；
  临时库已 drop；脚本幂等（连跑两次 diff 为空）。
  另注：原 initdb 无字段注释与 `--` 注释行，生成后自动带上，这是改进而非回归。
