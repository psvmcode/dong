# 2026-09-04

- comment-translation 团队任务：检查 crossborder / mq / replica / search 四个包下的英文 Javadoc。
  经逐包扫描与抽样核对，所有 Javadoc 已为中文，无需修改；执行 `mvn compile -DskipTests -q` 通过。
- team-lead 已用脚本全局修复注解顺序；二次确认负责范围内 97 个文件无英文 Javadoc 需翻译，编译通过。
- comment-translation 团队任务：翻译 redpacket / seckill / social / tcc 四个包下的英文 Javadoc。
  共修改 29 个文件，主要把 DTO/处理器/实现类中仅含英文标识符的 Javadoc 译成中文语义说明，编译通过并已提交推送。
- comment-translation 团队任务：检查并纠正负责范围内「注解在 Javadoc 之上」的顺序错误。
  用脚本扫描 redpacket/seckill/social/tcc 四个包后未发现成员级顺序错误；`mvn compile -DskipTests -q` 通过。已推送最新翻译提交。
- 项目纪律更新：所有 git 提交信息必须使用中文，英文 commit message 被禁止。我已记录到 MEMORY.md，并会告知队长此前两次英文提交以便修正。
- 团队协调：队长已要求先不要对历史英文提交做 rebase / force-push，需等用户明确同意。后续新提交使用中文 message 正常 commit + push。
- 应用户要求全局检查并纠正「注解在 Javadoc 之上」的顺序错误：用脚本扫描 src/main/java 全部 Java 文件，把原本位于注解下方的 Javadoc 上移到注解上方（统一为 Javadoc → 注解 → 代码），共修正 69 个文件；二次扫描确认剩余 0 处，`mvn compile -DskipTests -q` 通过。
- 上述顺序修正 + 已完成的翻译改动，以中文 commit message「修正 Javadoc 与注解顺序并翻译英文注释」提交并推送到 origin/master（commit e6317b9，77 个文件）。
- 遗留待用户确认：worker-business 此前两次英文 commit（e3672ac、2b84025）已推送到远端，改写需 rebase + force push，属不可逆操作，已请示用户，未擅自执行。
- 应用户要求提交代码：把上次脚本整改后残留的占位式 Javadoc（cache/limiter/lock/mq/redis 等 37 个文件，如 `isL1Enabled`、`@return true`、错拷的「删除关注关系」）替换为说明用途的中文描述并补齐 @param/@return；同时修正 `CacheInvalidationEvent` 中 `public /**` 的声明顺序。`mvn clean compile` 通过后以中文 commit「完善框架层与实验模块的 Javadoc 注释」提交并推送（commit ae224fc，37 个文件）。
- 调研结论：项目**未引入任何状态机框架**（无 spring-statemachine / COLA / squirrel 依赖），状态流转全部是「枚举 + Mapper updateStatus」手写式。唯一达到完整状态机三要素（合法迁移定义 + CAS 并发保护 + 终态幂等判定）的是跨境汇款 `RemittanceStatus`；其余 8 个状态枚举（SeckillOrder/SeckillActivity/RedPacket/Tcc×3/MqMessage/Product/CrossBorder 的 Account·FxQuote·Settlement）只是状态字段，updateStatus 多为无条件的 `set status=? where no=?`。
- 待办（未处理）：`src/main/java/com/dong/lab/tcc/mapper/TccStatusTypeHandler.java` 是 0 字节残留文件（08-29 delete_file 未真正删除的坑，已提交进版本库，且 git status 干净所以不会被发现）。实际用的是 `TccTransactionStatusTypeHandler.java`。
