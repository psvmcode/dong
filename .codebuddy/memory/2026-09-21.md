# 2026-09-21

## 新增 doc/learning/java/sort.md（commit `cf3a125`）

- 六种常用排序：冒泡、选择、插入、快排、归并、堆排序。每节一句思路 + 最简实现 + 特点，
  末尾给总览表、实测耗时、选型顺序、`Arrays.sort` 的双轴快排 / TimSort 分派。
- 代码**跑过再写进文档**：13 组用例（空、单元素、已排序、逆序、全相同、含负数、随机 2/3/10/100/1000/5000）
  与 `Arrays.sort` 结果全部一致。20 万随机数：快排 12ms、归并 19ms；2 万冒泡 121ms。
- java/README.md 里单独开了「算法」分组（并发那组放不下，也避免混淆）。
- ⚠️ 项目里还有 `doc/learning/algorithm/`（`templates.md`），排序按用户字面要求放在 java 目录下；
  若以后算法文档变多，应统一归到 algorithm 目录。
