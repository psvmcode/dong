# 2026-06-24 工作日志

## MCP Server Tool Demo 创建
- 基于项目已有的 Spring AI MCP 框架，新增了两个 Tool demo 服务：
  - `CalculatorService`: 加减乘除四则运算工具
  - `DateTimeService`: 获取当前时间、计算天数差、获取星期几
- 更新 `McpConfig` 注册所有 3 个 Tool 服务（OpenMeteoService + CalculatorService + DateTimeService）
- 将 MCP Server 从 stdio 模式切换为 SSE 模式，方便 HTTP 测试

## 版本升级 & 问题修复
- 旧版 `spring-ai-mcp-server-spring-boot-starter` (1.0.0-M6) 只支持 stdio，不支持 SSE
- 升级为 `spring-ai-starter-mcp-server-webmvc` (1.0.0)，支持 SSE/WebMVC
- 注解从 `@Tool/@ToolParam` 更新为 `@McpTool/@McpToolParam`
- 启用 `annotation-scanner` 自动发现 Tool，删除手动注册的 McpConfig
- 去掉 context-path（SSE 端点不受 context-path 影响）
- 删除 MCP Client 依赖（只保留 Server）
- 删除 .mvn 目录、.gitattributes、自我介绍.txt、mcp-servers-config.json

## MCP SSE 连接成功 ✅
- 最终方案：`spring-ai-starter-mcp-server-webmvc:1.0.0` + SSE 模式 + `@Tool`/`@ToolParam` 注解
- SSE 端点路径：`http://localhost:8080/sse`
- CodeBuddy MCP 配置：`{"url": "http://localhost:8080/sse"}`
- 成功调用 getAirQuality(武汉) → 返回天气信息
