# 经典 Redis 场景模块说明

> 本文覆盖 `com.dong.classic` 全部场景：业务场景、技术场景、接口清单与数据模型。
> 这些场景的共同点是**用 Redis 的某种数据结构解决一个具体的工程问题**，
> 本文重点讲清楚「为什么用这个结构」以及「Redis 之外还缺什么」。

---

## 目录

- [一、模块概览](#一模块概览)
- [二、业务场景](#二业务场景)
- [三、技术场景](#三技术场景)
- [四、接口文档](#四接口文档)
- [五、数据模型](#五数据模型)

---

## 一、模块概览

代码位于 `com.dong.classic`，对外前缀统一为 `/api/classic`。

| 场景 | 核心结构 | 解决的问题 |
|---|---|---|
| 短链接 | 发号器 + Base62 + 缓存 | 长链转短链，并统计点击 |
| 用户签到 | Bitmap | 每人每月几百字节存下整月签到 |
| 独立访客 | HyperLogLog | 上亿级去重只占 12KB |
| 排行榜 | ZSet | 高频更新下的实时排名 |
| 延迟队列 | RDelayedQueue | 超时未支付自动关单 |
| 地理位置 | GEO | 附近的人 / 附近的门店 |
| 发号器 | 四种策略对比 | 分布式 id 的取舍 |
| 锁与限流 | 分布式锁 / 令牌桶 | 并发控制 |

一个贯穿始终的原则：**Redis 负责高性能，数据库负责可靠性**。
任何只存在 Redis 里的业务数据都是不可靠的——它会过期、会丢、无法对账。
本模块每个场景都配了落库方案，见 [三、技术场景](#三技术场景)。

---

## 二、业务场景

### 2.1 短链接

```
生成：发号器 → 雪花 id → Base62 编码 → 短码
跳转：短码 → 查缓存 → 命中直接返回，未命中查库并回写缓存
```

设计要点：

- **同一长链每次生成不同短码**：用雪花 id 而非哈希，避免被批量遍历，也避免哈希冲突
- **跳转用 302 而非 301**：301 是永久重定向，会被浏览器缓存，之后再访问不再经过服务端，**点击统计会彻底失效**
- **缓存空值防穿透**：不存在的短码缓存一个 `null` 标记（10 分钟），第二次不会打到数据库

支持**过期时间**与**停用**：

| 能力 | 实现 | 为什么 |
|---|---|---|
| 过期 | 建链时设定 `expireTime`，缓存 TTL 跟着缩短 | 到期后缓存自动失效，请求落到数据库被过期校验拦下 |
| 停用 | 更新 `enabled` 并**同步删缓存** | 不删缓存的话，已缓存的短链仍会跳转成功，停用形同虚设 |

### 2.2 用户签到

Bitmap 实现：每人每月一个 key，第 N 天对应第 N 个位。

```
key   = lab:sign:{userId}:{yyyyMM}
offset = dayOfMonth - 1
```

**为什么用位图**：一个用户一整月的签到记录只占 31 位，约 4 字节；
一年下来也就几十字节。换成记录表是几百行。

**连续天数怎么算**：从当天往前扫位图，遇到 0 就停。跨月时要额外拼接上月末的连续段。

支持**补签**：补签与正常签到在位图里没有区别，但在流水表里用 `source` 字段区分，
运营统计时可以把补签单独算出来。

### 2.3 独立访客（UV）

HyperLogLog 实现：每个页面每天一个 key。

| 特点 | 说明 |
|---|---|
| 空间 | 固定约 12KB，与数据量无关 |
| 精度 | 约 **0.81% 误差** |
| 适用 | 看趋势，如「今天比昨天涨了多少」 |
| **不适用** | 金额、库存这类要求精确的去重 |

**注意**：HLL 给的是估算值，**落库时必须是覆盖而非累加**——累加会让数字虚高。

### 2.4 排行榜

ZSet 实现：member 是成员，score 是分数。

| 操作 | 复杂度 |
|---|---|
| 更新分数 | O(log N) |
| 查前 N 名 | O(log N + N) |
| 查某人名次 | O(log N) |

相比数据库 `order by score limit`，高频更新场景下代价低得多——数据库每次更新都要重排索引。

支持总榜与周榜分开存储，结算后的榜单按时间归档到历史榜。

### 2.5 延迟队列

Redisson `RDelayedQueue` 实现，适合「超时未支付自动关单」这类场景。

> **重要限制**：Redis 延迟队列**没有重试也没有持久化保证**。
> 进程重启或 Redis 故障都可能丢消息。强可靠场景应使用 RocketMQ 的延迟消息。

本模块的做法是**落库兜住可靠性**：

| 环节 | 动作 |
|---|---|
| 投递 | 先落库（状态=待投递）→ 再入队（状态=已投递） |
| 消费 | 取出后标记已消费并记录实际时间 |
| 补偿 | 定时任务扫描「预计时间已过 1 分钟仍未消费」的任务重新投递 |

**先落库再入队，顺序不能反**：入队成功后才落库的话，一旦落库失败就再也查不到这个任务了。

### 2.6 地理位置

Redis GEO 实现，底层是把经纬度编码成 score 存进 ZSet，再做范围查询。

按**城市分集合**（`lab:geo:{city}`），避免全量数据挤在一个 key 里。

### 2.7 发号器

四种策略对比：

| 策略 | 优点 | 缺点 |
|---|---|---|
| 雪花算法 | 趋势递增、本地生成、无网络开销 | 依赖机器时钟，时钟回拨会重复 |
| 号段模式 | 绝对递增、对数据库压力小 | 需要额外的号段表，有网络开销 |
| Redis INCR | 最简单 | 会暴露业务量，且依赖 Redis |
| UUID | 无协调 | 无序，做数据库主键会严重页分裂 |

### 2.8 锁与限流

| 能力 | 说明 |
|---|---|
| 分布式锁 | Redisson，带看门狗自动续期。**租期必须大于业务执行时间**，否则业务没跑完锁就被释放 |
| 限流 | 令牌桶，支持对比「有限流 vs 无限流」的通过量差异 |

---

## 三、技术场景

### 3.1 缓存与落库的分工

这是本模块最重要的一条设计原则：

| 数据 | Redis 的角色 | 数据库的角色 |
|---|---|---|
| 短链映射 | 高频读取缓存（7 天） | 权威存储 |
| 短链点击数 | 原子累加器 | **定时回写**（原实现只存 Redis，已修复） |
| 签到 | 位图，高性能判断 | 流水表，持久化与对账 |
| UV | HLL，估算 | 每日快照，查历史趋势 |
| 排行榜分数 | ZSet，实时排名 | 快照表，可重建 |
| 延迟任务 | 队列，延迟触发 | 任务表，追踪与补偿 |
| 坐标 | GEO，范围查询 | 位置表，持久化 |

**判断标准**：如果这份数据丢了，业务是否受影响？受影响就必须落库。

### 3.2 点击计数：为什么不在跳转时直接写库

短链跳转是**极高频率、极低延迟要求**的操作。每次跳转都 `update ... set hit_count = hit_count + 1` 会：

- 引入一次数据库写，跳转延迟从毫秒级升到几十毫秒
- 同一个热点短链的行锁会成为瓶颈

正确做法分两层：

```
跳转时：只累加 Redis 原子计数器（纯内存，微秒级）
定时：  批量把计数回写数据库
```

**回写用绝对值而非增量**：即使某一轮回写失败，下一轮也能自动补平，不会积累误差。

### 3.3 缓存一致性：删除而非更新

停用短链时**删除缓存**而不是更新缓存值。原因：

- 删除后下次请求自然会从数据库加载最新状态
- 更新缓存则可能与其他并发写产生顺序错乱

这也是「Cache Aside」模式的标准做法。

### 3.4 三大缓存问题

| 问题 | 现象 | 本模块的做法 |
|---|---|---|
| 穿透 | 查一个不存在的 key，每次都打到数据库 | 缓存空值标记（10 分钟） |
| 击穿 | 热点 key 过期瞬间大量请求打到数据库 | 短链缓存 7 天，且跳转极快 |
| 雪崩 | 大量 key 同时过期 | 空值标记 TTL 与正常值错开 |

### 3.5 幂等与失败降级

落库都采用**失败只记录日志、不抛异常**的策略：

```java
try {
    mapper.upsert(...);
} catch (Exception ex) {
    log.error("persist failed", ex);
}
```

原因：Redis 那一步已经成功，主流程已经完成。
不能因为落库这种辅助动作失败就让用户的操作失败——**手段不能比目的还重要**。

代价是可能出现短暂的数据不一致，由定时任务下一轮补平。

### 3.6 实验数据也值得落库

发号器、锁、限流看起来只是「跑个实验」，但它们的结论是有价值的数据：

| 实验 | 核心指标 | 落库后的价值 |
|---|---|---|
| 发号器 | 生成耗时 | 一次跑分说明不了问题，积累多次才能看出策略差异 |
| 锁 | **丢失更新数**（期望 − 实际） | 不必每次重跑，直接对比历史实验 |
| 限流 | **第二轮放行数** | 一轮突发四种算法放行量必然相同，差异只在第二轮 |

两个落库细节：

- **发号器按批次记而非按 id 记**：一次生成几万个 id 只落库一条。
  逐条落库的话，落库本身会成为瓶颈，反而干扰「对比发号性能」这个实验目的。
- **限流没做第二轮时记 -1 而不是 0**：0 表示「做了但一个都没放行」，
  -1 表示「根本没做」，两者含义不同，不能混淆。

实测一组（8 线程 × 20 循环）：期望 160，实际 3，**丢失 157 次更新**——
不加锁的后果比多数人预期的严重得多。

### 3.7 定时任务

| 任务 | 频率 | 作用 |
|---|---|---|
| 回写短链点击数 | 1 分钟 | 把 Redis 计数落库 |
| 补偿延迟任务 | 1 分钟 | 重投超时未消费的任务 |

---

## 四、接口文档

统一前缀 `/api/classic`。响应统一包装为 `Result<T>`。

### 4.1 短链接 `/short-link`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/short-link` | 生成短链，参数 url、expireMinutes（0 表示长期） |
| GET | `/short-link/resolve` | 解析短码并累加点击，参数 code |
| GET | `/short-link/detail` | 查询短链详情 |
| GET | `/short-link/hits` | 查询点击次数 |
| POST | `/short-link/toggle` | 启停短链，参数 code、enabled |
| POST | `/short-link/flush-hits` | 手工触发点击回写（定时任务也会做） |
| GET | `/short-link/s/{code}` | **跳转**，302 重定向 |

### 4.2 签到 `/sign`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/sign` | 签到，参数 userId |
| GET | `/sign` | 查询指定日期是否已签到 |
| GET | `/sign/streak` | 查询连续签到天数 |
| GET | `/sign/month` | 统计当月累计签到天数 |
| GET | `/sign/calendar` | 查询当月签到日历 |

### 4.3 独立访客 `/uv`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/uv/record` | 记录一次访问，参数 page、visitorId |
| GET | `/uv/count` | 查询当日估算独立访客数 |
| GET | `/uv/range` | 查询日期区间内的合并估算值 |

### 4.4 排行榜 `/rank`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/rank/submit` | 设置分数（覆盖），参数 board、member、score |
| POST | `/rank/add` | 累加分数，参数 board、member、delta |
| GET | `/rank/top` | 查询前 N 名 |
| GET | `/rank/rank` | 查询某成员名次 |
| GET | `/rank/score` | 查询某成员分数 |
| GET | `/rank/around` | 查询某成员前后若干名 |

### 4.5 实验记录 `/lab-record`

发号器、锁、限流这三个场景是对照实验，结果落库后可回看历史，不必每次重跑。

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/lab-record/id` | 查某发号策略的历史记录，参数 strategy、limit |
| GET | `/lab-record/lock` | 查某模式的锁实验历史，参数 mode（`no-lock`/`redisson-lock`）、limit |
| GET | `/lab-record/limiter` | 查限流算法对比历史，参数 bizKey、limit |

### 4.6 延迟队列与地理位置（挂在 `/api/classic` 下）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/delay-queue/offer` | 投递延迟任务，参数 payload、delaySeconds |
| GET | `/delay-queue/take` | 取出已到期任务 |
| GET | `/delay-queue/size` | 待消费任务数量 |
| POST | `/geo` | 添加坐标，参数 city、longitude、latitude、member |
| GET | `/geo/nearby` | 查询附近成员 |
| GET | `/geo/distance` | 计算两个成员的距离 |
| GET | `/id` | 按策略批量生成 id |
| GET | `/lock/without-lock` | 不加锁的并发累加（对照组） |
| GET | `/lock/with-lock` | 加锁的并发累加 |
| GET | `/limiter/try` | 尝试通过限流器 |
| GET | `/limiter/compare` | 对比有限流与无限流的通过量 |

---

## 五、数据模型

| 表 | 用途 |
|---|---|
| `short_url` | 短链接主表，含点击数、启用状态与过期时间 |
| `classic_signin_record` | 签到流水，位图丢失后可据此重建 |
| `classic_uv_daily` | 每日独立访客快照 |
| `classic_leaderboard_snapshot` | 排行榜分数持久化 |
| `classic_delay_task` | 延迟任务记录，用于追踪与补偿 |
| `classic_geo_place` | 地理位置持久化 |
| `classic_id_generated` | 发号器生成批次记录，用于对比四种策略性能 |
| `classic_lock_lab_result` | 锁对照实验结果，核心指标是丢失更新数 |
| `classic_rate_limit_lab_result` | 限流算法对比结果，重点是第二轮放行数 |

> `classic_id_generated` 的字段叫 `last_id` 而不是 `last_value`：
> `LAST_VALUE` 是 MySQL 的窗口函数保留字，直接用作列名会报语法错误。

完整 DDL 见 `db/schema.sql`。

> 改完 DDL 必须执行 `./deploy/gen-initdb.sh` 重新生成 `deploy/initdb/`。
> 这两个 initdb 是容器首次启动时用的「库内脚本」，无建库语句，**不要手工编辑**。

---

## 附：本地验证

```bash
# 启动（按需关闭不可用组件）
mvn spring-boot:run -Dspring-boot.run.arguments="--dong.elasticsearch.enabled=false --dong.mariadb.enabled=false"

# 生成短链（60 分钟后过期）
curl -X POST "http://127.0.0.1:8090/api/classic/short-link?url=https%3A%2F%2Fexample.com&expireMinutes=60"

# 点击几次后查看点击数
curl "http://127.0.0.1:8090/api/classic/short-link/hits?code={短码}"

# 手工触发回写，再看数据库的 short_url.hit_count
curl -X POST "http://127.0.0.1:8090/api/classic/short-link/flush-hits"
```
